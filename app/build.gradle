apply plugin: 'com.android.application'

android {
    signingConfigs {
        kiya {
            keyAlias 'kiya'
            keyPassword 'handk1314'
            storeFile file('/home/kiya/Workspace/keystore/kiya.jks')
            storePassword 'handk1314'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "space.kiya.flashlightsoul"
        minSdkVersion 14
        targetSdkVersion 23
        versionCode 1
        versionName '0.1'
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.1.1'
}
